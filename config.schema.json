{
  "pluginAlias": "AiSEG2",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "AiSEG2"
      },
      "autodiscover": {
        "title": "Auto discover AiSEG2 controllers",
        "type": "boolean",
        "default": false,
        "description": "Scans the Homebridge host's current local IPv4 subnets when Host is empty. A configured Host always takes precedence."
      },
      "host": {
        "title": "Hostname or IP",
        "type": "string",
        "required": false,
        "default": "",
        "description": "The hostname or IP address of the Panasonic AiSEG2 controller. Leave empty to use auto discovery."
      },
      "password": {
        "title": "Password",
        "type": "string",
        "required": true,
        "default": "",
        "description": "Password used to login to the Panasonic AiSEG2 controller"
      },
      "groupAirPurifierSensors": {
        "title": "Group air purifier sensors",
        "type": "boolean",
        "default": true,
        "description": "Marks each air purifier as the primary HomeKit service and links its air quality sensors and mode switches to it."
      },
      "groupAirConditionerSensors": {
        "title": "Group air conditioner sensors",
        "type": "boolean",
        "default": true,
        "description": "Marks each air conditioner as the primary HomeKit service and links its indoor humidity, outdoor temperature, fan, humidity, and extra mode services to it."
      },
      "groupAirEnvironmentSensors": {
        "title": "Group air environment sensors",
        "type": "boolean",
        "default": true,
        "description": "Marks each temperature service as the primary HomeKit service and links its paired humidity sensor to it."
      },
      "exposeContactSensorLockState": {
        "title": "Expose contact sensor lock state",
        "type": "boolean",
        "default": false,
        "description": "Adds a read-only Contact Sensor service to window lock sensors when AiSEG2 reports lock state data."
      },
      "echonetDiscovery": {
        "title": "ECHONET Lite discovery",
        "type": "boolean",
        "default": false,
        "description": "Logs ECHONET Lite devices found by multicast and unicast probing. This is diagnostic only and does not change AiSEG2 control behavior."
      },
      "echonetSubnets": {
        "title": "ECHONET Lite discovery subnets",
        "type": "string",
        "required": false,
        "default": "",
        "description": "Optional comma-separated /24 to /32 IPv4 targets for ECHONET Lite discovery, for example 192.168.20.0/24. Leave empty to scan the Homebridge host's current local IPv4 subnets."
      }
    }
  }
}
