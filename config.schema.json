{
  "pluginAlias": "AiSEG2",
  "pluginType": "platform",
  "singular": true,
  "customUi": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "AiSEG2"
      },
      "autodiscover": {
        "title": "Auto discover AiSEG2 controllers",
        "type": "boolean",
        "default": false,
        "description": "Scans the Homebridge host's current local IPv4 subnets when Host is empty. A configured Host always takes precedence."
      },
      "host": {
        "title": "Hostname or IP",
        "type": "string",
        "required": false,
        "default": "",
        "description": "The hostname or IP address of the Panasonic AiSEG2 controller. Leave empty to use auto discovery."
      },
      "password": {
        "title": "Password",
        "type": "string",
        "required": true,
        "default": "",
        "description": "Password used to login to the Panasonic AiSEG2 controller"
      },
      "groupAirPurifierSensors": {
        "title": "Group air purifier sensors",
        "type": "boolean",
        "default": true,
        "description": "Marks each air purifier as the primary HomeKit service and links its air quality sensors and mode switches to it."
      },
      "groupAirConditionerSensors": {
        "title": "Group air conditioner sensors",
        "type": "boolean",
        "default": true,
        "description": "Marks each air conditioner as the primary HomeKit service and links its indoor humidity, outdoor temperature, fan, humidity, and extra mode services to it."
      },
      "groupAirEnvironmentSensors": {
        "title": "Group air environment sensors",
        "type": "boolean",
        "default": true,
        "description": "Marks each temperature service as the primary HomeKit service and links its paired humidity sensor to it."
      },
      "groupEcocuteServices": {
        "title": "Group EcoCute services",
        "type": "boolean",
        "default": true,
        "description": "Marks the manual water-heating switch as the primary HomeKit service and links automatic bath and temperature services to it."
      },
      "exposeContactSensorLockState": {
        "title": "Expose contact sensor lock state",
        "type": "boolean",
        "default": false,
        "description": "Adds a read-only Contact Sensor service to window lock sensors when AiSEG2 reports lock state data."
      },
      "echonetDiscovery": {
        "title": "ECHONET Lite discovery",
        "type": "boolean",
        "default": false,
        "description": "Logs ECHONET Lite devices found by multicast and unicast probing. This is diagnostic only and does not change AiSEG2 control behavior."
      },
      "echonetSubnets": {
        "title": "ECHONET Lite discovery subnets",
        "type": "string",
        "required": false,
        "default": "",
        "description": "Optional comma-separated /24 to /32 IPv4 targets for ECHONET Lite discovery, for example 192.168.20.0/24. Leave empty to scan the Homebridge host's current local IPv4 subnets."
      },
      "echonet": {
        "title": "ECHONET Lite direct control",
        "type": "object",
        "properties": {
          "enabled": {
            "title": "Enable ECHONET Lite direct control",
            "type": "boolean",
            "default": false,
            "description": "Enables direct ECHONET Lite status/control for automatically matched shutters, door locks, air purifiers, and EcoCute devices. AiSEG2 remains the discovery source."
          },
          "subnets": {
            "title": "ECHONET Lite subnets",
            "type": "string",
            "default": "",
            "description": "Optional comma-separated /24 to /32 IPv4 targets, for example 192.168.20.0/24. Leave empty to scan the Homebridge host's current local IPv4 subnets."
          },
          "preferShutters": {
            "title": "Prefer ECHONET Lite for shutters",
            "type": "boolean",
            "default": true
          },
          "preferDoorLocks": {
            "title": "Prefer ECHONET Lite for door locks",
            "type": "boolean",
            "default": true
          },
          "preferAirPurifiers": {
            "title": "Prefer ECHONET Lite for air purifiers",
            "type": "boolean",
            "default": true
          },
          "preferEcocutes": {
            "title": "Prefer ECHONET Lite for EcoCute devices",
            "type": "boolean",
            "default": true
          },
          "fallbackToAiseg": {
            "title": "Fallback to AiSEG2 when ECHONET Lite fails",
            "type": "boolean",
            "default": false,
            "description": "If enabled, matched shutters, door locks, and air purifiers retry through AiSEG2 when direct ECHONET Lite requests fail. EcoCute direct control requires ECHONET Lite."
          },
          "doorLockHosts": {
            "title": "Advanced door lock host overrides",
            "type": "object",
            "default": {},
            "description": "Manual fallback map from AiSEG2 door lock display name to HF-JA1/JA2 IP address. Normal installs should leave this unset; automatic matching is used first."
          }
        }
      },
      "energy": {
        "title": "Energy status sensors",
        "type": "object",
        "properties": {
          "enabled": {
            "title": "Show energy status sensors in Apple Home",
            "type": "boolean",
            "default": false,
            "description": "Adds contact-sensor style services for Solar Surplus, Battery Ready, Battery Discharging, and EcoCute Good Time. Raw W/kWh values are logged only."
          },
          "solarSurplusWatts": {
            "title": "Solar surplus threshold watts",
            "type": "integer",
            "minimum": 0,
            "maximum": 100000,
            "default": 2500
          },
          "batteryReadyPercent": {
            "title": "Battery ready percent",
            "type": "integer",
            "minimum": 0,
            "maximum": 100,
            "default": 80
          },
          "batteryDischargeThresholdWatts": {
            "title": "Battery discharge threshold watts",
            "type": "integer",
            "minimum": 0,
            "maximum": 100000,
            "default": 100
          }
        }
      },
      "ecocuteSolarAutomation": {
        "title": "EcoCute solar automation",
        "type": "object",
        "properties": {
          "enabled": {
            "title": "Enable EcoCute solar automation",
            "type": "boolean",
            "default": false,
            "description": "Automatically starts manual EcoCute water heating when solar, battery, weather, and time-window conditions are met. It never sends stop."
          },
          "dryRun": {
            "title": "Dry run",
            "type": "boolean",
            "default": true,
            "description": "Logs when EcoCute would be started without sending the command."
          },
          "ecocuteName": {
            "title": "EcoCute",
            "type": "string",
            "default": "",
            "description": "Optional AiSEG2 EcoCute display name. Leave empty only when exactly one EcoCute exists."
          },
          "allowedStartTime": {
            "title": "Allowed start time",
            "type": "string",
            "default": "09:30",
            "description": "Local HH:mm time when automation may start."
          },
          "allowedEndTime": {
            "title": "Allowed end time",
            "type": "string",
            "default": "14:30",
            "description": "Local HH:mm time after which automation will not start."
          },
          "minSolarWatts": {
            "title": "Minimum solar watts",
            "type": "integer",
            "minimum": 0,
            "maximum": 100000,
            "default": 2500
          },
          "minBatteryPercent": {
            "title": "Minimum battery percent",
            "type": "integer",
            "minimum": 0,
            "maximum": 100,
            "default": 80
          },
          "requireBatteryNotDischarging": {
            "title": "Require battery not discharging",
            "type": "boolean",
            "default": true
          },
          "minBatteryChargeWatts": {
            "title": "Minimum battery charge watts",
            "type": "integer",
            "minimum": 0,
            "maximum": 100000,
            "default": 0,
            "description": "Optional stricter gate. Keep 0 to allow standby/low charging when other conditions are good."
          },
          "oncePerDay": {
            "title": "Start at most once per day",
            "type": "boolean",
            "default": true
          },
          "checkIntervalSeconds": {
            "title": "Check interval seconds",
            "type": "integer",
            "minimum": 60,
            "maximum": 3600,
            "default": 300
          },
          "weatherEnabled": {
            "title": "Use weather forecast",
            "type": "boolean",
            "default": false,
            "description": "Adds an Open-Meteo forecast gate using shortwave radiation, cloud cover, and precipitation probability."
          },
          "latitude": {
            "title": "Latitude",
            "type": "number",
            "minimum": -90,
            "maximum": 90,
            "default": 0,
            "description": "Use the custom settings UI location button to fill this from browser geolocation."
          },
          "longitude": {
            "title": "Longitude",
            "type": "number",
            "minimum": -180,
            "maximum": 180,
            "default": 0,
            "description": "Use the custom settings UI location button to fill this from browser geolocation."
          },
          "forecastHours": {
            "title": "Forecast hours",
            "type": "integer",
            "minimum": 1,
            "maximum": 12,
            "default": 3
          },
          "minForecastRadiationWatts": {
            "title": "Minimum forecast radiation W/m2",
            "type": "integer",
            "minimum": 0,
            "maximum": 1200,
            "default": 350
          },
          "maxForecastCloudCover": {
            "title": "Maximum forecast cloud cover percent",
            "type": "integer",
            "minimum": 0,
            "maximum": 100,
            "default": 85
          },
          "maxForecastPrecipitationProbability": {
            "title": "Maximum forecast precipitation probability percent",
            "type": "integer",
            "minimum": 0,
            "maximum": 100,
            "default": 70
          }
        }
      },
      "webhook": {
        "title": "Webhook lock trigger",
        "type": "object",
        "properties": {
          "enabled": {
            "title": "Enable webhook",
            "type": "boolean",
            "default": false,
            "description": "Starts a local HTTP endpoint for POST requests such as UniFi fingerprint triggers."
          },
          "port": {
            "title": "Webhook port",
            "type": "integer",
            "default": 18582
          },
          "bind": {
            "title": "Webhook bind address",
            "type": "string",
            "default": "0.0.0.0"
          },
          "publicHost": {
            "title": "Webhook public host",
            "type": "string",
            "default": "",
            "description": "Host or IP used only when logging the generated URL, for example 192.168.50.111."
          },
          "token": {
            "title": "Webhook token",
            "type": "string",
            "default": "",
            "description": "Secret URL token. Leave empty to auto-generate and persist one; the generated URL is printed in the Homebridge log."
          },
          "method": {
            "title": "Webhook HTTP method",
            "type": "string",
            "default": "post",
            "oneOf": [
              {
                "title": "POST only",
                "enum": ["post"]
              },
              {
                "title": "GET only",
                "enum": ["get"]
              },
              {
                "title": "GET or POST",
                "enum": ["any"]
              }
            ],
            "description": "HTTP method accepted by the webhook endpoint. POST is safer; use GET only for integrations that cannot send POST."
          },
          "action": {
            "title": "Webhook lock action",
            "type": "string",
            "default": "unlock",
            "oneOf": [
              {
                "title": "Unlock only",
                "enum": ["unlock"]
              },
              {
                "title": "Toggle lock/unlock",
                "enum": ["toggle"]
              }
            ],
            "description": "Unlock only always requests unlock. Toggle unlocks when locked and locks when unlocked."
          },
          "doorLockName": {
            "title": "Door lock name",
            "type": "string",
            "default": "",
            "description": "Optional AiSEG2 door lock display name. Leave empty when only one door lock exists."
          },
          "cooldownSeconds": {
            "title": "Webhook cooldown seconds",
            "type": "integer",
            "minimum": 0,
            "maximum": 3600,
            "default": 5,
            "description": "Ignores repeated webhook posts for this many seconds to avoid duplicate fingerprint events toggling the lock back."
          }
        }
      }
    }
  }
}
